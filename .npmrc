# Supply-chain hardening: don't install a package version until it has been
# public for at least this many minutes (24h). Blunts attacks that rely on a
# freshly-published compromised version being pulled before it's caught.
minimum-release-age=1440

# Keep peer deps explicit & installs deterministic.
auto-install-peers=true
