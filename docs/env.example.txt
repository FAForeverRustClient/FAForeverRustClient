# Copy this file to docs/env.txt and adjust local paths as needed.
# PowerShell, not a POSIX shell: `NAME=value command` is a parse error here,
# so each variable is its own `$env:` assignment before the command.
Set-Location "<path-to-FAForever-Client>"
$env:FAF_GAME_PATH = "C:\ProgramData\FAForever\bin\ForgedAlliance.exe"
$env:FAF_REPLAY_GAME_PATH = "C:\ProgramData\FAForever\replaydata\bin\ForgedAlliance.exe"
pnpm tauri dev

# The retail Forged Alliance install (the folder holding `gamedata\lua.scd`),
# written into `fa_path.lua` so the game can mount the base `.scd` archives
# plus `movies`, `sounds` and `fonts`. Normally detected: from the Java or
# Python client's configured install path, else from the usual
# `%ProgramFiles%` locations. Set this only when neither applies, e.g. a
# retail or Steam install on another drive. A wrong or missing value is what
# makes FA start with an unreadable menu, no audio and no unit animations;
# the launch log says which install was resolved.
#
# $env:FAF_GAME_INSTALL_DIR = "C:\Games\THQ\Gas Powered Games\Supreme Commander - Forged Alliance"

# Offline UI work: no browser login, and role-gated screens (the tournament
# organiser tools) reachable without holding the role. The roles reveal
# controls and authorise nothing; the FAF API still refuses every privileged
# call from an account that lacks them.
#
# $env:FAF_FAKE_AUTH = "1"
# $env:FAF_FAKE_ROLES = "TOURNAMENT_DIRECTOR"
# pnpm tauri dev
#
# Startup logs `using FAF_FAKE_ROLES` when the override is picked up. If that
# line is absent, the variable did not reach the process: `$env:` applies to
# the current PowerShell session only, so a new tab or window needs it again.

# Point the tournament tab at another faf-tournaments deployment: a local
# server, or a staging instance. Defaults to the live service.
#
# $env:TOURNEY_API_BASE = "http://localhost:3000"
# pnpm tauri dev

# Point the training tab at a published catalogue manifest. Unset, the tab uses
# the small catalogue shipped in the client and says so on screen; FAF's own
# tutorial API fills the library either way. The manifest is plain JSON in the
# shape of crates/faf-app/src/infra/training_catalogue.json, and its `links`
# block is where the training Discord invite and the forum category ids live.
#
# $env:FAF_TRAINING_CATALOGUE_URL = "https://example.invalid/faf-training.json"
# pnpm tauri dev

# The catalogue's repository, for maintaining it from inside the client. Both
# of these ship with working values (FAForeverRustClient/guides and the org's
# own OAuth app), so nothing needs setting; these overrides are for a fork, a
# test app, or a build with catalogue maintenance turned off.
#
# $env:FAF_GUIDES_REPO = "SomeoneElse/guides"
# $env:FAF_GUIDES_GITHUB_CLIENT_ID = ""     # empty: no sign-in offered
# pnpm tauri dev
