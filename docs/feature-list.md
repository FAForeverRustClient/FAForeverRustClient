feature-list
1) generating multiple mapgens at once
2) multiple tabs should be possible to have run imoultaneously e.g. chatwindow 50% playtab 50%.
3) Also generally (if youre note already doing this) i recommend checking out the options of the python client. It has insane customization options. Some very important things to me:
i can click create game and generate maps while in a lobby
switching of mapgen version
and this: Delete Generated Maps on Exit 
4)  to disable all ui mods per click and re-enable the same ones per click, so that you can easily watch old replays. (probably better to create a Preset-Selector) you can add all currently activated mod to a preset and save it
5) show if a simmod is a ranked/unranked mod - or: is the lobby a ranked/unranked lobby in the current configuration
6) have special "Tournament" games that can be later on filtered in the replay valut
7) advanced search option für replays etc
8) map-search based on Terrian features (wateramount, biome)
9) restore if session token is expired
